import React from 'react';
import ProjectList from '../components/ProjectList';

const ProjectsPage = () => {
  return (
    <div>
      <ProjectList />
    </div>
  );
};

export default ProjectsPage;
